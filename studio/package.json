{
  "name": "studio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Sanity Studio for the site. Kept as its own workspace so React never enters the Solid bundle.",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "typegen": "bun scripts/extract-schema.ts && sanity typegen generate"
  },
  "dependencies": {
    "@sanity/vision": "^6.9.2",
    "react": "^19.2.2",
    "react-dom": "^19.2.2",
    "sanity": "^6.9.2",
    "styled-components": "^6.1.15"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
