# Copy to studio/.env and fill in.
# Studio env vars must be prefixed SANITY_STUDIO_ to reach the browser.

# Find it at https://sanity.io/manage, or run `bunx sanity init` in this folder.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
